name: Benchmark tutorial

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/benchmark.yml"
      - "benchmarks/**"
      - "docs/benchmarks/**"
      - "examples/ppi_synthetic_benchmark.ipynb"
      - "src/**"
      - "pyproject.toml"
      - "MANIFEST.in"
  pull_request:
    paths:
      - ".github/workflows/benchmark.yml"
      - "benchmarks/**"
      - "docs/benchmarks/**"
      - "examples/ppi_synthetic_benchmark.ipynb"
      - "src/**"
      - "pyproject.toml"
      - "MANIFEST.in"

permissions:
  contents: read

jobs:
  tutorial:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install package and notebook execution dependencies
        run: python -m pip install '.[benchmark]' nbformat nbclient nbconvert ipykernel
      - name: Exercise the benchmark runner on a bounded panel
        run: python -m benchmarks.run_synthetic --suite smoke --output benchmark-output/smoke.json
      - name: Execute the complete tutorial from a fresh kernel
        run: >-
          python -m nbconvert --execute --to notebook
          --ExecutePreprocessor.timeout=180 --ExecutePreprocessor.kernel_name=python3
          --output ppi_synthetic_benchmark.executed.ipynb --output-dir benchmark-output
          examples/ppi_synthetic_benchmark.ipynb
      - name: Preserve executed tutorial and smoke results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: benchmark-tutorial
          path: benchmark-output/*
          if-no-files-found: error
